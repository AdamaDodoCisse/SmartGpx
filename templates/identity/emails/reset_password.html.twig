{% block subject %}{{ 'identity.email.reset_password.subject'|trans }}{% endblock %}

{% block body %}
    <p>{{ 'identity.email.reset_password.greeting'|trans }}</p>
    <p>
        <a href="{{ resetUrl }}">{{ 'identity.email.reset_password.cta'|trans }}</a>
    </p>
    <p>{{ 'identity.email.reset_password.expiry_notice'|trans }}</p>
{% endblock %}
